{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "extra rolls",
            "database": "${workspaceFolder:rollBot}/extraRolls.sql"
        }
    ]
    "editor.ruler": 80
}
